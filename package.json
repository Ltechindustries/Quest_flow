{
  "name": "questflow-ai-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "postinstall": "pip3 install -r backend/requirements.txt --break-system-packages || pip3 install -r backend/requirements.txt || pip install -r backend/requirements.txt || true",
    "dev": "concurrently \"BACKEND_PORT=5000 PYTHONPATH=. python3 backend/app.py\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "start": "PYTHONPATH=. python3 backend/app.py"
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
